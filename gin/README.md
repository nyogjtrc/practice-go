# Practice Go - Gin


## package manager: dep

`go get -u github.com/golang/dep/cmd/dep`

`dep ensure -v`

## build docker image

cmd already in makefile

`make build`

## run service

cmd already in makefile

`make run`
